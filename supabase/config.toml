[project]
# Set this to your Supabase project ref after running: supabase link --project-ref <ref>
# project_id = "your-project-ref"

# Note: the cleanup function runs on a daily schedule.
# Set it up in the Supabase dashboard under Edge Functions → cleanup → Schedule
# or via pg_cron after deployment.
